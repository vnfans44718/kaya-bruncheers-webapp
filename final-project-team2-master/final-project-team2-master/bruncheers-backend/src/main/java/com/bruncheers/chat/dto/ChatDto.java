package com.bruncheers.chat.dto;

import java.util.Date;


import com.bruncheers.chat.entity.Chat;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;

/*@Getter
@Setter
@AllArgsConstructor
@NoArgsConstructor
@Builder*/
public class ChatDto {
	/*	private Long chNo;
		private String chName;
		private String chPhone;
		private String chContent;
		*/
	
	/*
		@Override
		public String toString() {
			// TODO Auto-generated method stub
			return "ChatDto [chNo=" + chNo + ", chName=" + chName + ", chPhone=" + chPhone + ", chContent=" + chContent
					+ "]\n";
		}
	
		public static ChatDto toDto(Chat chat) {
			return ChatDto.builder().chNo(chat.getChNo()).chName(chat.getChName()).chPhone(chat.getChPhone())
					.chContent(chat.getChContent()).build();
	
		}
	*/
}
